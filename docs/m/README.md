# Vendored m.js runtime (for GitHub Pages)

Copy of `../../src` so the docs site is self-contained when published from the `/docs` folder.
Canonical library source remains the repo-root `src/` package.
